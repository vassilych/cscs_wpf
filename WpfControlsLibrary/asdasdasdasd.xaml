<UserControl x:Class="WpfControlsLibrary.asdasdasdasd"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wcl="clr-namespace:WpfControlsLibrary"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             >
    <wcl:ASDateEditer x:Name="dateEditer" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <wcl:ASDateEditer.Resources>
            <Style TargetType="{x:Type Button}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Viewbox>
                                <ContentPresenter/>
                            </Viewbox>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </wcl:ASDateEditer.Resources>
    </wcl:ASDateEditer>
</UserControl>
