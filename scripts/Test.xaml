<Window 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfCSCS"
        mc:Ignorable="d"
        Title="Title" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <TextBox DataContext="var1" VerticalAlignment="Top" HorizontalAlignment="Left"/>
        <Button Content="msg" DataContext="batun" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
        <Button Content="test" DataContext="testBut" VerticalAlignment="Bottom" HorizontalAlignment="Left"/>
        <DataGrid HorizontalAlignment="Center" VerticalAlignment="Center" Height="300" Width="300" DataContext="grid1" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="array0" />
                <DataGridTextColumn Header="array1" />
                <DataGridTextColumn Header="array2" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>

</Window>
